<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fivet.buddy.mapper.ChatRoomMapper">

    <!-- 팀 생성시 생성되는 기본채팅방-->
    <insert id="createTeam">
        insert into
            chat_room
        values
            (chat_room_seq.nextval, #{teamSeq}, #{teamName}, #{teamOwnerSeq}, 'basic', 1, sysdate)
        <selectKey order="AFTER" keyProperty="chatRoomSeq" resultType="String">
            select chat_room_seq.currval from dual
        </selectKey>
    </insert>

    <!--    팀 합류시(생성 포함) 기본적으로 추가되는 나와의 대화-->
    <insert id="insertSelfChat">
        insert into
            chat_room
        values
            (chat_room_seq.nextval, #{teamSeq}, '나와의 대화', #{memberSeq}, 'self', 1, sysdate)
        <selectKey order="AFTER" keyProperty="chatRoomSeq" resultType="String">
            select chat_room_seq.currval from dual
        </selectKey>
    </insert>

    <!-- 팀 및 회원 정보로 채팅방 목록을 출력 -->
    <select id="chatRoomList" resultType="com.fivet.buddy.dto.ChatRoomDTO">
        select
            cr.chat_room_seq, chat_title
        from
            chat_room cr inner join chat_member cm on cr.chat_room_seq = cm.chat_room_seq
        where
            cm.member_seq = #{memberSeq} and cm.team_seq = #{teamSeq}
        order by 1
    </select>
</mapper>