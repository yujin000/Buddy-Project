<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fivet.buddy.mapper.CalMapper">
    <insert id="insertEvent" >
        insert into EVENT values (
        event_seq.nextval,
        #{teamSeq},
        #{eventName},
        #{eventWriter} ,
        #{eventType},
        #{eventStart},
        #{eventEnd},
        #{eventLocation},
        #{eventMemo},
        #{eventColor}
        )
    </insert>

    <select id="selectAll" resultType="com.fivet.buddy.dto.CalDTO">
<!--        이름 상관없이 값이 1개만 넘어오면 value 로 처리가능-->
        select * from event where "team_seq" = #{teamSeq}
    </select>

    <delete id="deleteEvent">
        delete event where "event_seq" = #{value}
    </delete>

    <update id="updateEvent">
        update event set "event_type" = #{eventType}, "event_title" =#{eventTitle} ,
        "event_start"=#{eventStart}, "event_end" =#{eventEnd}, "event_location" =#{eventLocation}, "event_memo" =#{eventMemo},
        "event_color"=#{eventColor} where "event_writer" = "#{eventWriter}

    </update>

</mapper>